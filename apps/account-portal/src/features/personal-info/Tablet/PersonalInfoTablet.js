'use client';
import PersonalInfoDesktop from '../Desktop/PersonalInfoDesktop';

/**
 * Tablet View for Personal Info
 * Imports and renders PersonalInfoDesktop, sharing the unified card structure.
 */
export default function PersonalInfoTablet(props) {
  return <PersonalInfoDesktop {...props} />;
}
